# Moved

This document is now at **[deploy/nginx/LOCAL_DOCKER_DEPLOYMENT.md](deploy/nginx/LOCAL_DOCKER_DEPLOYMENT.md)**.

It covers:

- Mode A — direct `localhost` access
- Mode B — Ubuntu + Nginx + LAN subdomains
- Access URLs, demo users, seeded data
- Windows / Mac / Linux `hosts` file setup
- 502 troubleshooting

# Generated by: npm run env:sync (from repo root .env.docker)
# Do not copy this file manually — run env:sync after editing .env.docker
#
# Meeting-server-only optional overrides (add to .env.docker if needed):
#   RECORDINGS_DIR=./recordings
#   LOG_LEVEL=info
#   OPENAI_API_KEY=          # Whisper fallback
#   GCS_BUCKET=
# Guide: ../docs/ENV_SETUP.md

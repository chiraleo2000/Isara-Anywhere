# Database bundle (read-only copy)

Copied from platform `scripts/database/` via `npm run db:bundle:apps`.
Do not edit schema here — pin schemaVersion in docs/DATABASE.md and change platform SSOT only.
